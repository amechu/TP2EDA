#pragma once
#include "allegro.h"
#include "parseCmd.h"

void generateUniforme(parseData*, allegroUtils*);
void generateOctogono(parseData*, allegroUtils*);
void generateMandelbrot(parseData*, allegroUtils*);

